#Docker Notes

Docker is an open-source platform that simplifies building, shipping, and running applications by using containerization.

Essentially, Docker packages an application along with all its dependencies—such as libraries, system tools, and configurations—into a single, portable container. 

This container can run reliably across any environment that supports Docker, ensuring that what works on my development machine works everywhere else too.
